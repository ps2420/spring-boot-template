management:
  security:
    enabled: false
    
spring:
  application:
    name: service-registry
  boot:
    admin:
      context-path: /admin  
  cloud:
    config:
      uri: http://localhost:8081
        
security:
  ignored: /**
  basic:
    enabled: false
    
endpoints:
  jmx:
    enabled: false
    
info:
  version: ${APP_VERSION}
  name: ${spring.application.name}                